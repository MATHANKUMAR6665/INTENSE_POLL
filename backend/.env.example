# Server Configuration
PORT=8080

# MongoDB Connection String (Supports local or free MongoDB Atlas cluster)
MONGO_URI=mongodb://localhost:27017
MONGO_DB_NAME=syncpoll

# Redis Connection String (Supports local or free Upstash / Redis Cloud)
# Format: redis://localhost:6379 or rediss://default:password@host:port
REDIS_URI=redis://localhost:6379

# JWT Secret for Creator Authentication
JWT_SECRET=syncpoll_production_secret_key_guvi_hcl_2026

# CORS Allowed Origin
CORS_ORIGIN=*
